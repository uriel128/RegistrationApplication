# JavaFX Registration Form

A simple JavaFX registration form with regex-based validation for first name, last name, Farmingdale email, date of birth (`MM/DD/YYYY`), and ZIP code.

## Run

```bash
mvn javafx:run
```
